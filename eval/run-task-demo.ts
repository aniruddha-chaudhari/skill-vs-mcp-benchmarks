/**
 * Task Eval Demo (placeholder)
 *
 * The previous MockAgent demo relied on helpers that were removed from this repository.
 * Use `eval/run-eval.ts` to explore the evaluation framework instead.
 */

console.log("Run `bun run eval/run-eval.ts --list` to explore available tasks.");
